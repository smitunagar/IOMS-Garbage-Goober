<div class="lp-wrap">
  <div class="lp-center">

    <div class="lp-mascot-wrap">
      <img src="/images/Mascot.png" alt="Garbage Goober" class="lp-mascot-img">
    </div>

    <div class="lp-card">

      <div class="lp-logo-wrap">
        <img src="/images/logo-auth.png" alt="Garbage Goober" class="lp-logo-img">
      </div>

      <h2 class="lp-card-sub-heading"><%= t('resetPasswordTitle') %></h2>

      <% if (flash && flash.error) { %>
        <div class="alert alert-danger mb-3" role="alert">
          <i class="bi bi-exclamation-circle me-1"></i><%= flash.error %>
        </div>
      <% } %>

      <form method="POST" action="/reset-password">
        <input type="hidden" name="token" value="<%= token %>">

        <div class="input-float mb-3">
          <input type="password" name="newPassword" id="resetPw"
                 class="form-control" placeholder=" " required autofocus>
          <label for="resetPw">
            <i class="bi bi-lock me-1"></i><%= t('newPasswordLabel') %>
          </label>
        </div>

        <div class="input-float mb-4">
          <input type="password" name="confirmPassword" id="resetPwConfirm"
                 class="form-control" placeholder=" " required>
          <label for="resetPwConfirm">
            <i class="bi bi-lock-fill me-1"></i><%= t('confirmPasswordLabel') %>
          </label>
        </div>

        <p class="text-muted small mb-3"><%= t('passwordHelperMinChars') %></p>

        <button type="submit" class="btn btn-login w-100">
          <%= t('resetPasswordButton') %>
          <i class="bi bi-arrow-right ms-2"></i>
        </button>
      </form>

    </div><!-- /.lp-card -->

    <div class="lp-wm-credit">
      <img src="/images/logo2.png" alt="WebMeister360" class="lp-wm-logo">
    </div>

  </div>
</div>
